name: Security

# Phase 3 deliverable - currently a stub.
# Will run bandit, pip-audit, and trivy on the binary.

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * 1"  # Mondays 06:00 UTC

permissions:
  contents: read

jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Stub
        run: echo "Security pipeline not yet implemented (Phase 3)."
